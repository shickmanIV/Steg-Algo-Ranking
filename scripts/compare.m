


% Take an image and embed a message using each steg algo


% For each resulting struct of embedded images, calculate the 
% imperceptibility metrics (PSNR, SSID, MSE). Save the results into a data
% table.